# SVCFusion Studio 使用说明

## 钢琴窗音高线

钢琴窗会显示两条音高线：

- 输出音高线：推理声码器时使用的实际输出音高。
- SVC 音高线：推理 SVC 模型时使用的音高。

在绘制模式中：

- 按住左键绘制输出音高线。
- 按住右键绘制 SVC 音高线。
- 按住 Ctrl 绘制输出音高线时，被绘制位置会回归 MIDI 基准音高。
- 按住 Ctrl 绘制 SVC 音高线时，被绘制位置会回归当前声码器输出音高。
